export default ({
    Vue, // the version of Vue being used in the VuePress
    options, // the options for the root Vue instance
    router, // the router instance of the app 
    siteData, // site metadata
}) => {
    // ..apply enhancements to the app
}